server:
  port: 9999
spring:
  application:
    name: DesafioCompasso
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  jmx:
    enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/compasso_data_base?allowPublicKeyRetrival=true&sslMode=DISABLED
    username: root
    password:
